{
  "name": "elitea-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "ConvoQA auto-generated Playwright (TypeScript) UI tests",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "lint": "echo \"(optional) add eslint\""
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "dotenv": "^16.4.5",
    "typescript": "^5.4.0"
  }
}
